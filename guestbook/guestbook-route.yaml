apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app.kubernetes.io/instance: guestbook
  name: guestbook-route
  namespace: guestbook
spec:
  host: guestbook.ocpcommops-np.cloud.aep.com
  port:
    targetPort: 8080
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: commops-qa-etrm-deal-processor-unreg-service
    weight: 100
  wildcardPolicy: None
